# MAKEIT install and setup procedures

### Running the *install.sh* script

To get started with *makeit* in your project, just run the *install.sh* script
like so :

```Shell Session
$ ./install.sh PROJECT_NAME PROJECT_ROOTDIR
```
